<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>basic</artifactId>
        <groupId>cn.iocoder.boot</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>basic-module-mall</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <description>
        商城大模块，由 product 商品、promotion 营销、trade 交易、statistics 统计等组成
    </description>
    <modules>
        <module>basic-module-product</module>
        <module>basic-module-promotion</module>
        <module>basic-module-trade</module>
        <module>basic-module-statistics</module>
        <!--
            特殊：为什么会有 basic-module-trade-api 呢？
                basic-module-promotion 和 basic-module-trade 之间相互循环依赖，所以抽出 basic-module-trade-api 模块，这样：
                1. basic-module-promotion 依赖 basic-module-trade-api
                2. basic-module-trade 依赖 basic-module-promotion
            从而不存在相互（循环）依赖，即 basic-module-trade => basic-module-promotion => basic-module-trade-api
         -->
        <module>basic-module-trade-api</module>
    </modules>

</project>
